/// Shared between onboarding (where it's first picked) and Settings
/// (where it can be changed later) so the two never drift apart.
const checkInFrequencyOptions = ['Once a day', 'Twice a day', 'Every 3 hours', 'Every hour'];
